# iRis-first mode and fallbacks
IRIS_MODE=1
LOCAL_UPLOAD_DIR=var/uploads

# Optional external extractor (leave unset for local fallback)
# EXTRACTOR_BASE_URL=http://localhost:8001

# Dev-only API key. If set, send x-api-key: this-value to sensitive endpoints
INTERNAL_API_KEY=dev-local-only-123
